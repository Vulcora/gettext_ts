name: Publish to Hex.pm

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.15'
          otp-version: '26'

      - name: Restore dependencies cache
        uses: actions/cache@v3
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Run tests
        run: mix test

      # Pushing main without a version bump must not fail the pipeline —
      # only publish when the version is new on hex.pm.
      - name: Check if version already published
        id: guard
        run: |
          VERSION=$(mix run --no-start -e 'IO.puts(Mix.Project.config()[:version])' | tail -1)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if mix hex.package fetch gettext_ts "$VERSION" -o /tmp/probe.tar 2>/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "gettext_ts $VERSION is already on hex.pm — skipping publish."
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to Hex.pm (public)
        if: steps.guard.outputs.published == 'false'
        run: mix hex.publish --yes
        env:
          HEX_API_KEY: ${{ secrets.HEX_VULCORA_KEY }}
